/*
    file:   directoryList.js
    Auth:   Jesse Parnell
    Desc:   Gets a list of directories within the zoneminder image export.
*/

define([], () => {
    const directoryList = () => {
        let dirDB = new micronDB();

        //get the directory listings.
        //store all data in DB.

        return dirDB;
    }

    return directoryList;
});